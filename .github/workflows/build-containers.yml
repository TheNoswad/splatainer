name: Build and Push Containers (Buildah)

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * *'  # daily at midnight UTC
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: thenoswad/splatainer

permissions:
  contents: read
  packages: write

jobs:
  build-cuda:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: redhat-actions/podman-login@v1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Buildah Build (CUDA)
        id: build
        uses: redhat-actions/buildah-build@v2.9
        with:
          image: ${{ env.IMAGE_NAME }}
          tags: cuda latest ${{ github.sha }}-cuda
          containerfiles: |
            ./Containerfile.cuda
          context: .
          oci: true
          archs: amd64
          extra-args: |
            --pull
            --security-opt label=disable
            --label org.opencontainers.image.source=https://github.com/${{ github.repository }}
            --label org.opencontainers.image.description="COLMAP/GLOMAP container with NVIDIA CUDA support"

      - name: Push to GHCR
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build.outputs.image }}
          tags: ${{ steps.build.outputs.tags }}
          registry: ${{ env.REGISTRY }}
